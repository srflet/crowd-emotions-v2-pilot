import React, { Component } from 'react'

export default class PracticeEnd extends Component {
    render() {
        return (
            <div>
                This is the end of the practice round. Now you will do the actual round where you will be show the face array 3 times instead of two.
            </div>
        )
    }
}
